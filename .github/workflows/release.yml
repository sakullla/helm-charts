name: Release Charts

on:
  push:
    branches:
      - main
    paths:
      - 'charts/**'

permissions:
  contents: write # 允许发布 Release 和推送代码

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # 必须获取完整历史以检测变更

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Install Helm
        uses: azure/setup-helm@v3

      - name: Install yq
        uses: dcarbone/install-yq-action@v1.1.0 # 安装 yq 用于处理 yaml

      # 1. 发布过程：chart-releaser 会自动检测版本变化，只发布更新的 Chart
      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.5.0
        with:
          charts_dir: charts
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

      # 2. 修复版：清理 index.yaml (强制同步最新并在本地裁剪)
      - name: Prune index.yaml (Keep Only Latest)
        shell: bash
        run: |
          echo "🔄 Fetching latest gh-pages..."
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
          
          # 强制拉取远程 gh-pages 分支的最新状态
          git fetch origin gh-pages
          
          # 强制切换并重置到 origin/gh-pages (确保包含上一步 chart-releaser 生成的最新记录)
          git checkout -B gh-pages origin/gh-pages
          
          if [ -f index.yaml ]; then
            echo "👀 Before pruning (Count per chart):"
            yq '.entries[] | length' index.yaml | head -n 5
            
            echo "✂️ Pruning index.yaml to keep only the latest version per chart..."
            # yq: 遍历 entries 下的所有数组，删除索引 1 及之后的内容
            yq -i 'del(.entries[][1:])' index.yaml
            
            echo "👀 After pruning (Should be 1):"
            yq '.entries[] | length' index.yaml | head -n 5
            
            # 检查是否有变化
            if [[ -n $(git status -s) ]]; then
              git add index.yaml
              git commit -m "chore: prune index.yaml to latest version only [skip ci]"
              
              # 使用 Token 显式推送到远程
              git push https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git gh-pages
              echo "✅ index.yaml pruned and forced pushed."
            else
              echo "✅ index.yaml is already clean."
            fi
          else
            echo "⚠️ No index.yaml found on gh-pages branch!"
          fi
          
          # 切回 main 分支 (如果后续步骤需要)
          git checkout main

      # 3. 清理旧版本 Release/Tag：只保留最近 2 个
      # 仅针对本次 Push 涉及的 Chart 进行检查
      - name: Cleanup Old Releases (Keep last 2)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 获取本次提交变更的 Chart 列表
          # git diff 比较当前 SHA 与上一次提交，过滤 charts/ 目录，提取一级目录名
          CHANGED_CHARTS=$(git diff --name-only HEAD^ HEAD | grep '^charts/' | cut -d/ -f2 | sort -u)

          if [ -z "$CHANGED_CHARTS" ]; then
            echo "🤔 No charts modified in this push. Checking all charts just in case..."
            CHANGED_CHARTS=$(ls -d charts/*/ | xargs -n 1 basename)
          fi

          echo "📋 Target Charts for cleanup: $CHANGED_CHARTS"

          for CHART_NAME in $CHANGED_CHARTS; do
            # 再次确认目录存在（防止删除文件的情况）
            if [ ! -d "charts/$CHART_NAME" ]; then continue; fi

            echo "🧹 Checking cleanup for Chart: $CHART_NAME ..."

            # 列出该 Chart 的所有 Release，按时间倒序
            # tail -n +3: 保留前2个(第1,2行)，从第3个开始选中用于删除
            RELEASES_TO_DELETE=$(gh release list --limit 100 --json tagName --jq ".[] | select(.tagName | startswith(\"$CHART_NAME-\")) | .tagName" | tail -n +3)

            if [ -z "$RELEASES_TO_DELETE" ]; then
              echo "  ✅ $CHART_NAME has 2 or fewer releases. No cleanup needed."
            else
              echo "  🗑️ Deleting old releases (keeping top 2):"
              echo "$RELEASES_TO_DELETE"
              
              # 批量删除 Release 和 Tag
              echo "$RELEASES_TO_DELETE" | xargs -I {} gh release delete "{}" --cleanup-tag -y
            fi
          done