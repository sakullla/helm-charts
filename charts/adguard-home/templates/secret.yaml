apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "adguard-home.fullname" . }}-secret
  labels:
    {{- include "adguard-home.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

