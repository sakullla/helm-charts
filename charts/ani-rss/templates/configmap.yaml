apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ani-rss.fullname" . }}-config
  labels:
    {{- include "ani-rss.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}