apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "ani-rss.fullname" . }}-secret
  labels:
    {{- include "ani-rss.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

