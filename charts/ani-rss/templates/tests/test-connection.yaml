apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ani-rss.fullname" . }}-test-connection"
  labels:
    {{- include "ani-rss.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ani-rss.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
