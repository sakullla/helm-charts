apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "astrbot.fullname" . }}-config
  labels:
    {{- include "astrbot.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

