apiVersion: v2
name: automem
description: AutoMem long-term memory API with built-in FalkorDB resources and official Qdrant Helm dependency.

type: application

# Chart version
version: 0.0.17

# Application version
appVersion: "0.14.0"

dependencies:
  - name: falkordb
    version: "0.0.1"
    repository: https://sakullla.github.io/helm-charts
    condition: falkordb.enabled
  - name: qdrant
    version: "1.17.0"
    repository: https://qdrant.github.io/qdrant-helm
    condition: qdrant.enabled
