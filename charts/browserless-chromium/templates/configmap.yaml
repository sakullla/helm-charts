apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "browserless-chromium.fullname" . }}-config
  labels:
    {{- include "browserless-chromium.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

