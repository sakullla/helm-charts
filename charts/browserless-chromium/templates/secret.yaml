apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "browserless-chromium.fullname" . }}-secret
  labels:
    {{- include "browserless-chromium.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

