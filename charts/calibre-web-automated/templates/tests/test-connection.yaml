apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calibre-web-automated.fullname" . }}-test-connection"
  labels:
    {{- include "calibre-web-automated.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calibre-web-automated.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

