apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "certimate.fullname" . }}-config
  labels:
    {{- include "certimate.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

