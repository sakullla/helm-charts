apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certimate.fullname" . }}-test-connection"
  labels:
    {{- include "certimate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "certimate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

