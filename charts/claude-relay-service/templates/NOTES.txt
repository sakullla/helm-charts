{{- $fullName := include "claude-relay-service.fullname" . -}}
{{- $name := include "claude-relay-service.name" . -}}
🚀 Claude Relay Service 已部署!

{{- if not .Values.secrets.JWT_SECRET }}
⚠️  警告: 未设置 JWT_SECRET! 请运行以下命令更新:
   kubectl patch secret {{ $fullName }}-secret -n {{ .Release.Namespace }} --type merge -p '{"stringData":{"JWT_SECRET":"YOUR_32_CHAR_RANDOM_STRING"}}'
{{- end }}

{{- if not .Values.secrets.ENCRYPTION_KEY }}
⚠️  警告: 未设置 ENCRYPTION_KEY! 请运行以下命令更新:
   kubectl patch secret {{ $fullName }}-secret -n {{ .Release.Namespace }} --type merge -p '{"stringData":{"ENCRYPTION_KEY":"YOUR_32_CHAR_KEY"}}'
{{- end }}

📋 部署信息:
   命名空间: {{ .Release.Namespace }}
   版本: {{ .Chart.AppVersion }}
{{- if .Values.valkey.enabled }}
   Valkey: 已启用 (内置缓存)
{{- else }}
   Valkey: 未启用 (请确保外部 REDIS_HOST 可达)
{{- end }}

🔗 访问方式:
{{- if .Values.httpRoute.enabled }}
{{- if .Values.httpRoute.hostnames }}
   域名: {{ .Values.httpRoute.hostnames | first }}
{{- end }}
   HTTPRoute 已启用，请确保 Gateway API 控制器已安装
{{- else if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
{{- range .paths }}
   URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
{{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
   export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ $fullName }})
   export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
   echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
   NOTE: 负载均衡器 IP 可能需要几分钟才能分配
   kubectl get --namespace {{ .Release.Namespace }} svc -w {{ $fullName }}
   export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ $fullName }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
   echo http://$SERVICE_IP:{{ .Values.service.port }}
{{- else if contains "ClusterIP" .Values.service.type }}
   kubectl port-forward svc/{{ $fullName }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
   然后访问: http://127.0.0.1:8080
{{- end }}

🔧 常用命令:
   # 查看 Pod 状态
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ $name }}

   # 查看日志
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ $name }} -f

   # 进入管理界面 (需设置 ADMIN_USERNAME 和 ADMIN_PASSWORD)
   # 默认访问 /admin 路径

📖 配置说明:
   - 环境变量 (ConfigMap): {{ $fullName }}-config
   - 敏感信息 (Secret): {{ $fullName }}-secret
   - 健康检查: /health (端口 {{ .Values.service.port }})
