apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claude-relay-service.fullname" . }}-secret
  labels:
    {{- include "claude-relay-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}

