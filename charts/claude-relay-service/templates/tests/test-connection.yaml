apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "claude-relay-service.fullname" . }}-test-connection"
  labels:
    {{- include "claude-relay-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "claude-relay-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
