# Default values for claude-relay-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: weishaw/claude-relay-service
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 3000

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
  - name: gateway
    sectionName: http
    # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
  - chart-example.local
  # List of rules and filters applied.
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# 鍋ュ悍妫€鏌ユ帰閽堥厤缃?
# 鍙傝€?docker-compose.yml 涓殑 healthcheck 閰嶇疆
# interval: 30s, timeout: 10s, retries: 3
livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 10
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3
readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 30
  timeoutSeconds: 10
  failureThreshold: 3
# 鍚姩鎺㈤拡 - 浣跨敤 tcpSocket 妫€娴嬬鍙ｆ槸鍚﹀氨缁?
# 闃叉搴旂敤鍚姩鎱㈡椂琚繃鏃╅噸鍚紝鍙渶绔彛鐩戝惉鍗冲彲璁や负鍚姩鎴愬姛
startupProbe:
  tcpSocket:
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 6  # 6 * 10s = 60s 鍚姩鏃堕棿

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

persistence:
  enabled: false
  accessMode: ReadWriteOnce
  size: 5Gi
  storageClass: ""

# Valkey 渚濊禆閰嶇疆 (Redis 鐨勫紑婧愭浛浠?
# 鍚敤鍚庡皢鑷姩閮ㄧ讲 Valkey 瀹炰緥
# Chart: https://github.com/valkey-io/valkey-helm
valkey:
  enabled: true
  # 绂佺敤 replica 浠ヤ娇鐢?standalone 妯″紡
  replica:
    enabled: false
  auth:
    enabled: false
  # standalone 妯″紡涓嬬殑鎸佷箙鍖栭厤缃娇鐢?dataStorage
  dataStorage:
    enabled: true
    requestedSize: 1Gi

# 鐜鍙橀噺閰嶇疆锛堥潪鏁忔劅锛?
# 浼氳鍐欏叆 ConfigMap 骞舵寕杞藉埌瀹瑰櫒涓?
env:
  # 馃寪 鏈嶅姟鍣ㄩ厤缃?
  NODE_ENV: "production"
  PORT: "3000"
  HOST: "0.0.0.0"
  # 馃敡 璇锋眰浣撳ぇ灏忛厤缃?
  REQUEST_MAX_SIZE_MB: "60"
  # 馃攼 瀹夊叏閰嶇疆锛堥潪鏁忔劅閮ㄥ垎锛?
  ADMIN_SESSION_TIMEOUT: "86400000"
  API_KEY_PREFIX: "cr_"
  # 馃搳 Valkey/Redis 閰嶇疆
  # 濡傛灉浣跨敤鍐呯疆 Valkey 渚濊禆锛宒eployment 浼氳嚜鍔ㄨ鐩?REDIS_HOST
  # 濡傛灉浣跨敤澶栭儴 Redis/Valkey锛岃璁剧疆涓哄疄闄呯殑涓绘満鍦板潃
  REDIS_HOST: "valkey"
  REDIS_PORT: "6379"
  REDIS_DB: "0"
  # REDIS_ENABLE_TLS: ""
  # 馃幆 Claude API 閰嶇疆
  CLAUDE_API_URL: "https://api.anthropic.com/v1/messages"
  CLAUDE_API_VERSION: "2023-06-01"
  CLAUDE_BETA_HEADER: "claude-code-20250219,oauth-2025-04-20,interleaved-thinking-2025-05-14,fine-grained-tool-streaming-2025-05-14"
  # 馃寪 浠ｇ悊閰嶇疆
  DEFAULT_PROXY_TIMEOUT: "60000"
  MAX_PROXY_RETRIES: "3"
  PROXY_USE_IPV4: "true"
  # PROXY_KEEP_ALIVE: ""
  # PROXY_MAX_SOCKETS: ""
  # PROXY_MAX_FREE_SOCKETS: ""
  # 馃搱 浣跨敤闄愬埗
  DEFAULT_TOKEN_LIMIT: "1000000"
  # 馃摑 鏃ュ織閰嶇疆
  LOG_LEVEL: "info"
  LOG_MAX_SIZE: "10m"
  LOG_MAX_FILES: "5"
  # 馃敡 绯荤粺閰嶇疆
  CLEANUP_INTERVAL: "3600000"
  TOKEN_USAGE_RETENTION: "2592000000"
  HEALTH_CHECK_INTERVAL: "60000"
  TIMEZONE_OFFSET: "8"
  # 馃帹 Web 鐣岄潰閰嶇疆
  WEB_TITLE: "Claude Relay Service"
  WEB_DESCRIPTION: "Multi-account Claude API relay service"
  WEB_LOGO_URL: "/assets/logo.png"
  # 馃洜锔?寮€鍙戦厤缃?
  DEBUG: "false"
  ENABLE_CORS: "true"
  TRUST_PROXY: "true"

# 鏁忔劅淇℃伅閰嶇疆
# 浼氳鍐欏叆 Secret 骞舵寕杞藉埌瀹瑰櫒涓?
# 鈿狅笍 蹇呭～: JWT_SECRET (鑷冲皯32瀛楃鐨勯殢鏈哄瓧绗︿覆), ENCRYPTION_KEY (32瀛楃鐨勫姞瀵嗗瘑閽?
# Sensitive environment variables
secrets: {}
  # JWT_SECRET: "your-jwt-secret-min-32-characters-long"
  # ENCRYPTION_KEY: "32-char-encryption-key-required"
  # REDIS_PASSWORD: ""
  # ADMIN_USERNAME: ""
  # ADMIN_PASSWORD: ""

# 棰濆鐜鍙橀噺锛堢敤浜庤鐩?ConfigMap 涓殑閰嶇疆鎴栨坊鍔犳柊鍙橀噺锛?
extraEnv: {}
  # CUSTOM_VAR: "custom-value"

