apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cognee.fullname" . }}-config
  labels:
    {{- include "cognee.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

