apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "ddns-go.fullname" . }}-secret
  labels:
    {{- include "ddns-go.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

