apiVersion: v1
kind: Service
metadata:
  name: {{ include "ddns-go.fullname" . }}
  labels:
    {{- include "ddns-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ddns-go.selectorLabels" . | nindent 4 }}
