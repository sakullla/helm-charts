{{- if .Values.deerflowConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "deer-flow.fullname" . }}-deerflow-config
  labels:
    {{- include "deer-flow.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.deerflowConfig | nindent 4 }}
{{- end }}
