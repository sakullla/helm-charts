apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dns-server.fullname" . }}-config
  labels:
    {{- include "dns-server.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}