apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "dns-server.fullname" . }}-secret
  labels:
    {{- include "dns-server.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

