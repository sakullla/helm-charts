{{- if and .Values.playwright.enabled .Values.playwright.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "playwright.fullname" . }}
  labels:
    {{- include "playwright.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "playwright.fullname" . }}
  minReplicas: {{ .Values.playwright.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.playwright.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.playwright.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.playwright.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.playwright.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.playwright.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
