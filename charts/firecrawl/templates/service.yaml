apiVersion: v1
kind: Service
metadata:
  name: {{ include "firecrawl.fullname" . }}
  labels:
    {{- include "firecrawl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firecrawl.selectorLabels" . | nindent 4 }}
