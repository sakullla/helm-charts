apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frp.fullname" . }}-config
  labels:
    {{- include "frp.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

