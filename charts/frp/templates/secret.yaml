apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "frp.fullname" . }}-secret
  labels:
    {{- include "frp.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

