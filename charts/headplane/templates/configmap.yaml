apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "headplane.fullname" . }}-config
  labels:
    {{- include "headplane.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.global.headplaneConfig | .Values.headplaneConfig | nindent 4 }}