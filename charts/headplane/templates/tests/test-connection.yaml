apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "headplane.fullname" . }}-test-connection"
  labels:
    {{- include "headplane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "headplane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
