apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "headscale.fullname" . }}-secret
  labels:
    {{- include "headscale.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

