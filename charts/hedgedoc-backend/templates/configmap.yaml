apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hedgedoc-backend.fullname" . }}-config
  labels:
    {{- include "hedgedoc-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

