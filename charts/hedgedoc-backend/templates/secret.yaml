apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hedgedoc-backend.fullname" . }}-secret
  labels:
    {{- include "hedgedoc-backend.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}

