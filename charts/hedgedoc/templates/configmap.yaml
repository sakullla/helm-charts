apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hedgedoc.fullname" . }}-config
  labels:
    {{- include "hedgedoc.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}