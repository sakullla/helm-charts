apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "hedgedoc.fullname" . }}-secret
  labels:
    {{- include "hedgedoc.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

