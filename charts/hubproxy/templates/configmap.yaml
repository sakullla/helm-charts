apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hubproxy.fullname" . }}-config
  labels:
    {{- include "hubproxy.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

