apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "hubproxy.fullname" . }}-secret
  labels:
    {{- include "hubproxy.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

