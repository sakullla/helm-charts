apiVersion: v1
kind: Service
metadata:
  name: {{ include "hubproxy.fullname" . }}
  labels:
    {{- include "hubproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hubproxy.selectorLabels" . | nindent 4 }}
