apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "hugo-site.fullname" . }}-secret
  labels:
    {{- include "hugo-site.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

