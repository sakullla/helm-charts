apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kavita.fullname" . }}-config
  labels:
    {{- include "kavita.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

