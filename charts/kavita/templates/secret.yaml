apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "kavita.fullname" . }}-secret
  labels:
    {{- include "kavita.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

