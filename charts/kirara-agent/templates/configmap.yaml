apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kirara-agent.fullname" . }}-config
  labels:
    {{- include "kirara-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

