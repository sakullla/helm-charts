apiVersion: v1
kind: Service
metadata:
  name: {{ include "kirara-agent.fullname" . }}
  labels:
    {{- include "kirara-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kirara-agent.selectorLabels" . | nindent 4 }}

