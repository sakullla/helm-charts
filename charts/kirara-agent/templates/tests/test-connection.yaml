apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kirara-agent.fullname" . }}-test-connection"
  labels:
    {{- include "kirara-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kirara-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
