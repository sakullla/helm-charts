apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "koipy.fullname" . }}-secret
  labels:
    {{- include "koipy.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

