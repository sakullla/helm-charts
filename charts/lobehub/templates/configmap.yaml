apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lobehub.fullname" . }}-config
  labels:
    {{- include "lobehub.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

