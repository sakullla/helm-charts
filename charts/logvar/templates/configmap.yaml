apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "logvar.fullname" . }}-config
  labels:
    {{- include "logvar.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

