apiVersion: v1
kind: Service
metadata:
  name: {{ include "logvar.fullname" . }}
  labels:
    {{- include "logvar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logvar.selectorLabels" . | nindent 4 }}

