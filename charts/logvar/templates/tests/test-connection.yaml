apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logvar.fullname" . }}-test-connection"
  labels:
    {{- include "logvar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logvar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

