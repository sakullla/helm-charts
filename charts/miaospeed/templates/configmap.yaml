apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "miaospeed.fullname" . }}-config
  labels:
    {{- include "miaospeed.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

