apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "misaka-danmu-server.fullname" . }}-config
  labels:
    {{- include "misaka-danmu-server.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

