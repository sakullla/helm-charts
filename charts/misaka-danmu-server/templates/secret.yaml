apiVersion: v1
kind: Secret
metadata:
  name: {{ include "misaka-danmu-server.fullname" . }}-secret
  labels:
    {{- include "misaka-danmu-server.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}

