apiVersion: v1
kind: Service
metadata:
  name: {{ include "misaka-danmu-server.fullname" . }}
  labels:
    {{- include "misaka-danmu-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "misaka-danmu-server.selectorLabels" . | nindent 4 }}
