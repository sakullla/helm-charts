apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "misaka-danmu-server.fullname" . }}-test-connection"
  labels:
    {{- include "misaka-danmu-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "misaka-danmu-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
