apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "misaka_danmu_server.fullname" . }}-config
  labels:
    {{- include "misaka_danmu_server.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}