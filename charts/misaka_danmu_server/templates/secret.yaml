apiVersion: v1
kind: Secret
metadata:
  name: {{ include "misaka_danmu_server.fullname" . }}-secret
  labels:
    {{- include "misaka_danmu_server.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}