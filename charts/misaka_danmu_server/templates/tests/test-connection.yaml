apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "misaka_danmu_server.fullname" . }}-test-connection"
  labels:
    {{- include "misaka_danmu_server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "misaka_danmu_server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
