apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "misub.fullname" . }}-config
  labels:
    {{- include "misub.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

