apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "misub.fullname" . }}-test-connection"
  labels:
    {{- include "misub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "misub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

