apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "newapi.fullname" . }}-config
  labels:
    {{- include "newapi.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

