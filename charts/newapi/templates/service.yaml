apiVersion: v1
kind: Service
metadata:
  name: {{ include "newapi.fullname" . }}
  labels:
    {{- include "newapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newapi.selectorLabels" . | nindent 4 }}
