apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "next-ai-draw-io.fullname" . }}-secret
  labels:
    {{- include "next-ai-draw-io.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

