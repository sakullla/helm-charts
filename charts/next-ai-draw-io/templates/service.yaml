apiVersion: v1
kind: Service
metadata:
  name: {{ include "next-ai-draw-io.fullname" . }}
  labels:
    {{- include "next-ai-draw-io.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "next-ai-draw-io.selectorLabels" . | nindent 4 }}
