apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next-ai-draw-io.fullname" . }}-test-connection"
  labels:
    {{- include "next-ai-draw-io.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next-ai-draw-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
