apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openlist.fullname" . }}-config
  labels:
    {{- include "openlist.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

