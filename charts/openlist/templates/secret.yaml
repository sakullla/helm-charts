apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "openlist.fullname" . }}-secret
  labels:
    {{- include "openlist.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

