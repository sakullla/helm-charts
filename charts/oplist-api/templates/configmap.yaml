apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "oplist-api.fullname" . }}-config
  labels:
    {{- include "oplist-api.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

