apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "oplist-api.fullname" . }}-secret
  labels:
    {{- include "oplist-api.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

