apiVersion: v1
kind: Service
metadata:
  name: {{ include "oplist-api.fullname" . }}
  labels:
    {{- include "oplist-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oplist-api.selectorLabels" . | nindent 4 }}

