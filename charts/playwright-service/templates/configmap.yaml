apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "playwright-service.fullname" . }}-config
  labels:
    {{- include "playwright-service.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

