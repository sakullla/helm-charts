apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "playwright-service.fullname" . }}-secret
  labels:
    {{- include "playwright-service.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

