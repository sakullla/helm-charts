apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "qbittorrent.fullname" . }}-secret
  labels:
    {{- include "qbittorrent.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

