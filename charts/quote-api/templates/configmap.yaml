apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quote-api.fullname" . }}-config
  labels:
    {{- include "quote-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
