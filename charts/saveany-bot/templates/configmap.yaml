apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "saveany-bot.fullname" . }}-config
  labels:
    {{- include "saveany-bot.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

