apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "saveany-bot.fullname" . }}-secret
  labels:
    {{- include "saveany-bot.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

