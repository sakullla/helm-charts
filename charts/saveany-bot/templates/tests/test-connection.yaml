apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "saveany-bot.fullname" . }}-test-connection"
  labels:
    {{- include "saveany-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "saveany-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
