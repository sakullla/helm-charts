apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "substore.fullname" . }}-test-connection"
  labels:
    {{- include "substore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "substore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
