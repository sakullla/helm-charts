apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tabby-web.fullname" . }}-config
  labels:
    {{- include "tabby-web.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

