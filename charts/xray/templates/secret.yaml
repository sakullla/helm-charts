apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "xray.fullname" . }}-secret
  labels:
    {{- include "xray.labels" . | nindent 4 }}
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

